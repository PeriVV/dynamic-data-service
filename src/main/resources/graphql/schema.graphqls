scalar JSON

schema { query: Query, mutation: Mutation }

type Query {
  health: String!
}

type Mutation {
  createUser(name: String!, email: String!): MutationResult!
  updateUser(name: String, email: String, id: ID!): MutationResult!
  deleteUserById(id: ID!): MutationResult!
}

type MutationResult {
  affected: Int!
  success: Boolean!
}
